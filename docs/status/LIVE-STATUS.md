# Live Status

Series One calibration is in progress. The model slate has passed direct API
smoke checks, the existing test suite passes, and the Episode 1 draft validates
with all 13 planned turns.

Next executable step:

```bash
ai-roundtables run \
  notes/when-should-you-trust-an-ai-answer/config.json \
  --output-dir runs/raw/when-should-you-trust-an-ai-answer-001
```

GitHub pushes remain disabled by the user's standing instruction.
